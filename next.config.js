/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  styledComponents: true,
  images: {
    domains: ['khimshportfolio.herokuapp.com'],
  },
}

module.exports = nextConfig
